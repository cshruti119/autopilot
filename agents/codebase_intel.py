from pathlib import Path

def index_repo(repo_path: str):
    # Brownfield codebase indexing — not needed for greenfield projects.
    # Re-implement with a vector DB when working with existing repos.
    pass

def query_context(question: str) -> str:
    return ""
